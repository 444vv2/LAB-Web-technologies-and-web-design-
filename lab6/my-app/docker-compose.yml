version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: lab9-postgres
    restart: unless-stopped
    environment:
      # These defaults match python-service/.env
      POSTGRES_DB: ${DB_NAME:-React-app-db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres123}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lab9-backend
    restart: unless-stopped
    working_dir: /app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME:-React-app-db}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres123}
      - RELOAD=true

  # frontend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: lab9-frontend
  #   restart: unless-stopped
    # ports:
    #   - "3000:3000"
    # volumes:
    #   - ./src:/app/src
    #   - ./public:/app/public
    #   - /app/node_modules
    # environment:
    #   - REACT_APP_API_URL=http://localhost:8000
    #   - CHOKIDAR_USEPOLLING=true
    # depends_on:
    #   - backend
volumes:
  pgdata: